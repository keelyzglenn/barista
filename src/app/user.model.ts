export class User {
  constructor (public name: string, public email: string, public position: string, public company: string, public experience: number, public description: string, public image: string) { }
}
